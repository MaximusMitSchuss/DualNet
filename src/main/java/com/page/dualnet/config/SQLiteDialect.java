package com.page.dualnet.config;

/**
 * Placeholder class: the project currently does not require a custom Hibernate dialect.
 * Previously a custom SQLite dialect caused compilation issues; we've reverted to H2 for now.
 */
public final class SQLiteDialect {
    // Intentionally empty - not used in current configuration.
}
